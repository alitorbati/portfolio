import React, { Component } from 'react';
import styled from 'styled-components';

export default styled('h2')`
  font-weight: 700;
  font-size: 3em;
  line-height: 1;
  margin-bottom: 20px;
  position: sticky;
  top: 0;
`
